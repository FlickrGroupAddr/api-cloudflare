# Reviewed contract mirror

`fail-polite-worker-database-conformance.md` is an exact, derived copy of the
accepted architecture contract at commit `c811a13`. Its SHA2-256 fingerprint is
checked by the release verifier against `src/release_contract.ts`.

Canonical source:
https://github.com/FlickrGroupAddr/architecture-design/blob/c811a13/docs/testing/fail-polite-worker-database-conformance.md

The architecture repository remains the decision authority. Update this mirror
only alongside a reviewed canonical amendment and the runtime/verifier pins.
Relative architecture links inside the byte-for-byte copy resolve in the
canonical repository. The mirror lets CI verify the reviewed contract without
requiring access to the private architecture repository.
